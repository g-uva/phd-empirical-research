# eGPU experiments

The five draft experiments represent the identifiable result families in the
checked-in plotting artifact. They are not claims of local reproduction.

```bash
python3 scripts/experiment_versions.py update --paper egpu exp-0001 \
  --reason "Register or update an eGPU experiment"
```

Create new experiments for actual raw-data collection; do not overwrite the
drafts or treat regenerated hard-coded plots as scientific reproduction.
